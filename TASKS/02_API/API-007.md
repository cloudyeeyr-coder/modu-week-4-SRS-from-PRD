---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[API] API-007: 에스크로 상태 조회 (GET /status) DTO 스펙"
labels: 'api, backend, escrow, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [API-007] 에스크로 TX 상태 조회 (`GET /api/escrow/[txId]/status`) Route Handler DTO
- 목적: 대시보드 외부 위젯이나 프론트 폴링 용도, 혹은 단순 REST 상태 체크를 위한 Get 요청의 파라미터 및 반환 규격 정의.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `06_SRS-v1.md#6.1` (API No. 3)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] Dynamic Routing `[txId]` 에 대한 Request Path Validator 인터페이스 (Type) 정의
- [ ] Response Payload: `txId`, `currentState`(`held` | `released`), `contractStatus`의 복합 객체.
- [ ] 에러 반환 DTO: 잘못된 txId일 경우(`404 Not Found`), 권한 없을 경우(`403`) 메시지

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 정상 상태 Get 리턴 포맷
- Given: 인가된 사용자의 조회 요청
- When: 올바른 txId 경로로 접근함
- Then: JSON 반환 값 내에 Enum String으로 된 명료한 에스크로 상태를 응답하여야 함. (예: `{ data: { state: 'held' }}`)

## :gear: Technical & Non-Functional Constraints
- 성능 검토: 향후 트래픽 증가 여부에 따라, 아주 단순한 조회 콤포넌트이므로 Edge Runtime 적용을 위한 호환성(Edge Functions 타입 지원 여부) 체크 제안.

## :checkered_flag: Definition of Done (DoD)
- [ ] GET 인터페이스를 구현할 때 참고할 수 있는 Response Interface 파일(.d.ts 또는 타입선언)이 존재.
- [ ] 모든 Acceptance Criteria 조건 통과

## :construction: Dependencies & Blockers
- Depends on: #DB-006
- Blocks: #FQ-004
