---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-016: ROBOT_MODEL 테이블 스키마 작성"
labels: 'database, schema, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-016] `ROBOT_MODEL` 엔티티 생성
- 목적: RaaS 데이터 비교 계산기(FQ)에서 서빙할 기준 모델 마스터 데이터를 저장.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 참조 요건: [`06_SRS-v1.md#REQ-FUNC-018`](../../06_SRS-v1.md) — RaaS 옵션 계산식 기반재료 

## :white_check_mark: Task Breakdown (실행 계획)

### Phase A: 로봇 제품 마스터 모델
- [ ] `Manufacturer` 테이블 연계형 확장 
  ```prisma
  model RobotModel {
    id             String   @id @default(cuid())
    modelCode      String   @unique
    modelName      String
    manufacturerId String
    priceInfo      Json?    // 일시불 단가, 월 리스료 단가율 등 복합 정책
    createdAt      DateTime @default(now())
    updatedAt      DateTime @updatedAt

    manufacturer Manufacturer @relation(fields: [manufacturerId], references: [id])
  }
  ```

### Phase B: 적용 구문 작성
- [ ] `npx prisma format`
- [ ] `npx prisma migrate dev --name init_robot_model`

## :test_tube: Acceptance Criteria (BDD/GWT)

**Scenario 1: 마스터 데이터 정합성**
- Given: 정의된 RobotModel 스키마
- When: 로컬 환경 마이그레이션 및 Mock DB Insert
- Then: DB 정상 생성 및 `Manufacturer` 매핑 보장.

## :gear: Technical & Non-Functional Constraints
- **유연한 가격 메타데이터:** 제품의 금융 가격 정책은 매우 자주 변하고(할인율, 옵션율), SQLite 제약 대응 상 복잡 모델(1:N) 대신 하나의 `PriceInfo` JSON 필드로 우회 관리.

## :checkered_flag: Definition of Done (DoD)
- [ ] `modelCode` Unique 플래그 
- [ ] `Manufacturer` FK 연결 

## :construction: Dependencies & Blockers
- **Depends on:** `#DB-004`(Manufacturer)
- **Blocks:** `#API-020`(계산기)
