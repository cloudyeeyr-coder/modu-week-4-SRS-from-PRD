---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-008: BADGE 테이블 스키마 작성"
labels: 'database, schema, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-008] `BADGE` 테이블 스키마 및 마이그레이션 작성
- 목적: 로봇 제조사가 SI 파트너에게 부여하는 브랜드 파트너십 인증서(뱃지)의 수명 주기를 제어하기 위한 스키마 설계.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 스키마 요건: [`06_SRS-v1.md#6.2.7`](../../06_SRS-v1.md) — BADGE 테이블 명세
- 기능 시나리오: [`06_SRS-v1.md#REQ-FUNC-014`](../../06_SRS-v1.md) — 만료 및 철회 비노출

## :white_check_mark: Task Breakdown (실행 계획)

### Phase A: 맵핑 디자인 및 모델 정의
- [ ] `schema.prisma` 파일 내 `Badge` 도메인 스키마 신규 작성
- [ ] 비정규화 정보 유지 목적의 `manufacturerName` (캐시 형태) 컬럼 지정
  ```prisma
  model Badge {
    id               String   @id @default(cuid())
    manufacturerId   String
    siPartnerId      String
    manufacturerName String   // 조인 코스트 감소용 비정규화 데이터
    issuedAt         DateTime @db.Date
    expiresAt        DateTime @db.Date
    isActive         Boolean  @default(true)
    revokedAt        DateTime?
    createdAt        DateTime @default(now())

    manufacturer Manufacturer @relation(fields: [manufacturerId], references: [id])
    siPartner    SiPartner    @relation(fields: [siPartnerId], references: [id])
  }
  ```

### Phase B: 포맷팅 & 마이그레이션 기록
- [ ] `npx prisma format` 구문 체크 
- [ ] 마스크립트 생성 (`npx prisma migrate dev --name init_badge`)

## :test_tube: Acceptance Criteria (BDD/GWT)

**Scenario 1: N:M 연결 관계 제약(교차 테이블)**
- Given: Manufacturer 레코드와 SiPartner 레코드
- When: 두 개의 부모 레코드를 식별키로 삽입할 때
- Then: 외래 키가 상호 관계 무결성 조건을 보장하는지 확인. 

## :gear: Technical & Non-Functional Constraints
- **읽기 최적화:** 뱃지 조회 시 불필요한 `Manufacturer` 조인을 피하기 위해 `manufacturerName`을 뱃지 생성 시 정적 복제하는 비정규화 전략을 적용함. DB의 디스크를 소모하여 트래픽 응답시간(p95 ≤ 1) 스펙(CON)을 충족시키는 구조.

## :checkered_flag: Definition of Done (DoD)
- [ ] `expiresAt`, `isActive` 필드 등의 기간/상태 로직 필드가 준비되었는가?
- [ ] 비정규화 필드인 `manufacturerName` 포함 여부 확인

## :construction: Dependencies & Blockers
- **Depends on:** `#DB-003`(SiPartner), `#DB-004`(Manufacturer)
- **Blocks:** `#API-016`(발급 API)
