---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-002: BUYER_COMPANY 테이블 스키마 작성"
labels: 'database, schema, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-002] `BUYER_COMPANY` 테이블 스키마 및 마이그레이션 작성
- 목적: 플랫폼에서 수요 기업 회원의 정보를 저장하고 관리하기 위한 데이터베이스 스키마(PK, UNIQUE 제약, ENUM)를 생성한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서 데이터 모델: `06_SRS-v1.md#6.2.1-BUYER_COMPANY-(수요-기업)`
- 통합 ERD: `06_SRS-v1.md#6.2.12-ER-Diagram`

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] `segment` 상태값을 나타내는 Enum 타입 정의 (`Q1`, `Q2`, `Q3`, `Q4`)
- [ ] `schema.prisma` 파일에 `BuyerCompany` 모델 정의 (id는 cuid/uuid 자동생성)
- [ ] `bizRegistrationNo` 필드에 `@unique` 제약 조건 추가 (비즈니스 식별자 단일성 보장)
- [ ] `createdAt`, `updatedAt` 등의 감사용 타임스탬프 필드 설정
- [ ] Prisma 마이그레이션 파일 오프라인 생성 (`npx prisma migrate dev --name init_buyer_company`)

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 모델 및 필드 정상 생성
- Given: 정의된 Prisma 스키마 구조
- When: 데이터베이스 마이그레이션을 실행함
- Then: `BuyerCompany` 테이블이 모든 명세된 필드와 PK를 가지고 DB에 오류 없이 생성된다.

Scenario 2: 데이터 무결성 검증 (Unique)
- Given: 기존에 삽입된 로우의 `bizRegistrationNo`가 `123-45-67890`임
- When: 동일한 `123-45-67890` 값을 가진 새로운 기업 정보를 INSERT 하려 함
- Then: `@unique` 제약 조건 위반(Conflict)을 발생시켜 데이터 중복을 방지해야 한다.

## :gear: Technical & Non-Functional Constraints
- 성능/안정성: ID 값은 시스템 내부 연계 확장을 위해 CUID/UUID 규칙을 사용하여 유추 불가능성과 분산 키 무결성을 보장해야 함 (SRS 6.2 모델링 노트 참고).

## :checkered_flag: Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria를 충족하는가?
- [ ] `schema.prisma` 파일이 오류 없이 포맷팅 및 빌드 되는가?
- [ ] 스키마 변경 사항에 대한 마이그레이션 파일이 레포지토리에 추가되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-001 (Prisma 설정)
- Blocks: #DB-005, #API-001, #MOCK-001
