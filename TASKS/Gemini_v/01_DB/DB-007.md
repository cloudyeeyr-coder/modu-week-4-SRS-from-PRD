---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-007: AS_TICKET 테이블 스키마 작성"
labels: 'database, schema, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-007] `AS_TICKET` 테이블 스키마 및 마이그레이션 작성
- 목적: 긴급 AS 유지보수 로컬망 연결 및 이벤트 기록을 위한 데이터 구조 설계. 보고된 이슈와 시간축(timeline)에 따른 상태 타임스탬프를 명확히 설계하여 SLA 충족률 계산(FQ-005)의 기반이 되도록 한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 테이블 명세: [`06_SRS-v1.md#6.2.6`](../../06_SRS-v1.md) — AS_TICKET 모델 속성
- 비즈니스 요구사항: [`06_SRS-v1.md#REQ-FUNC-008`](../../06_SRS-v1.md) — SLA 모니터링 (24시간 내 해결)

## :white_check_mark: Task Breakdown (실행 계획)

### Phase A: 타임라인 컬럼 구조화
- [ ] `priority` Enum 구조화 (`normal`, `urgent`)
- [ ] 모델 타임라인 기록 필드 구성 (`reportedAt`, `assignedAt`, `dispatchedAt`, `resolvedAt`) 및 `slaMet` Boolean 플래그 매핑

### Phase B: Prisma 모델 정의
- [ ] `schema.prisma` 파일 내 AS 도메인 모델 생성
  ```prisma
  model AsTicket {
    id                 String    @id @default(cuid())
    contractId         String
    priority           String    @default("normal")
    symptomDescription String
    assignedEngineerId String?   // 로컬 파견 AS 기사
    reportedAt         DateTime  @default(now())
    assignedAt         DateTime?
    dispatchedAt       DateTime?
    resolvedAt         DateTime?
    slaMet             Boolean?  
    createdAt          DateTime  @default(now())

    contract Contract @relation(fields: [contractId], references: [id])
  }
  ```

### Phase C: 컴파일 및 마이그레이션 레코드
- [ ] `npx prisma format` 정상 여부 체크
- [ ] `npx prisma migrate dev --name init_as_ticket` 마이그레이션 파일 작성

## :test_tube: Acceptance Criteria (BDD/GWT)

**Scenario 1: SLA 타임스탬프 필드 정합성 체크**
- Given: 정의된 데이터베이스 스키마 
- When: 로컬 환경 DB Sync 및 Push 실행
- Then: `slaMet` 플래그가 Nullable Boolean 으로 생성되고, 각 `...At` 타임스탬프가 Nullable로 유연하게 허용됨을 확인. Exit code = 0

## :gear: Technical & Non-Functional Constraints
- **Audit 설계:** 시스템 특성상 각 시간의 누락은 신뢰도와 직결됨. DB 단에서는 Nullable(`?`)로 허가하되 어플리케이션(API-011) Layer에서 상태 전이 시 필수 값으로 보장하게 연계됨.

## :checkered_flag: Definition of Done (DoD)
- [ ] 타임라인 속 필드 4종(`reportedAt`, `assignedAt`, `dispatchedAt`, `resolvedAt`) 필드가 생성되었는가?
- [ ] `Contract`와 N:1 연결이 완료되었는가?

## :construction: Dependencies & Blockers
- **Depends on:** `#DB-005` (계약)
- **Blocks:** `#DB-017` (AS 기사 테이블 마스터), `#API-009` (생성 API)
