---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-006: ESCROW_TX 테이블 스키마 작성"
labels: 'database, schema, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-006] `ESCROW_TX` 테이블 스키마 및 마이그레이션 작성
- 목적: 에스크로 거래 상태 및 5년 데이터 보존을 충족하기 위한 관리자 개입 이력(admin memo)을 로깅하는 트랜잭션 테이블을 생성한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `06_SRS-v1.md#6.2.5-ESCROW_TX-(에스크로-내역)`
- 제약사항: `06_SRS-v1.md#CON-08` (전자금용거래법 준수 - 5년 보존)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] `EscrowState` Enum 정의 (`held`, `released`, `refunded`)
- [ ] `schema.prisma`에 `EscrowTx` 모델 추가
- [ ] `contractId` 필드에 1:1 관계(`@unique`) 외래키 설정
- [ ] 금액(`amount`) Decimal 필드 생성
- [ ] 타임스탬프(`adminVerifiedAt`, `releasedAt`) 및 어드민 식별/기록(`adminMemo`) 필드 추가
- [ ] 마이그레이션 파일 생성 (`init_escrow_tx`)

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 1:1 관계 무결성 검증
- Given: 계약(Contract A)이 존재함
- When: 두 개의 별도 트랜잭션이 동일한 Contract A를 가리키는 `EscrowTx` 레코드를 생성하려고 시도함
- Then: 두 번째 삽입은 `@unique` 외래키 제약조건 위반으로 차단된다.

Scenario 2: Enum 전이 통제 
- Given: 활성화된 DB 엔진
- When: `state`를 모델에 없는 `pending`이나 `in_progress` 등으로 삽입함
- Then: Enum Validation 오류 발생으로 결제 오기록을 방어한다.

## :gear: Technical & Non-Functional Constraints
- 보안/신뢰성: 레코드 삭제(Hard Delete)를 방지하기 위한 전략 적용(Soft Delete) 또는 어플리케이션 계층에서의 DELETE 차단 보장 필요.
- 안정성: 금액(`amount`) 필드 Prisma Decimal 타입 적용 준수.

## :checkered_flag: Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria 충족
- [ ] `schema.prisma` 포맷팅 완료
- [ ] Git 커밋에 `.sql` 단위 마이그레이션 파일이 포함되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-005 (Contract)
- Blocks: #DB-011, #API-004, #API-005, #API-007
