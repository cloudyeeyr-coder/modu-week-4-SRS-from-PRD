---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-012: QUOTE_LEAD 테이블 스키마 작성"
labels: 'database, schema, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-012] `QUOTE_LEAD` (수기 견적 요청 리드) 테이블 스키마 및 마이그레이션 작성
- 목적: 자동 RaaS 비교 이후 심화 견적을 요청하기 위해 남긴 사용자 폼 데이터를 적재 (영업 리드 수집용). 

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 스키마 요건: [`06_SRS-v1.md#6.2.11`](../../06_SRS-v1.md) — QUOTE_LEAD

## :white_check_mark: Task Breakdown (실행 계획)

### Phase A: 유연한 소속 릴레이션 선언
- [ ] `buyerCompanyId`를 인증/비인증 상태를 고려하여 Nullable 옵션으로 구성
  ```prisma
  model QuoteLead {
    id               String   @id @default(cuid())
    buyerCompanyId   String?  // 비회원 견적도 허용할 시 Nullable 연결
    robotModel       String
    quantity         Int
    termMonths       Int
    contactName      String
    contactEmail     String
    contactPhone     String
    status           String   @default("pending") // pending, in_progress, responded, closed
    responseData     Json?    // 관리자의 커스텀 견적서 첨부 등
    adminRespondedAt DateTime?
    createdAt        DateTime @default(now())
    updatedAt        DateTime @updatedAt

    buyerCompany BuyerCompany? @relation(fields: [buyerCompanyId], references: [id])
  }
  ```

### Phase B: CLI 마이그레이션
- [ ] `npx prisma format`
- [ ] `npx prisma migrate dev --name init_quote_lead`

## :test_tube: Acceptance Criteria (BDD/GWT)

**Scenario 1: 비회원 Lead 수집 가능성 검토**
- Given: 정의된 스키마.
- When: 웹 사이트에 로그인하지 않고 익명의 사용자가 연락처만을 기입한 채 견적을 요청함.
- Then: `BuyerCompanyId` Null 허용 룰을 바탕으로 정상 Insert 된다.

## :gear: Technical & Non-Functional Constraints
- **인증 의존성 분리:** 세일즈 퍼널의 진입 장벽을 낮추기 위해 `BuyerCompany` 참조키를 강제(Required)하지 않도록 설계하여 리포지토리의 저장 성공률을 높인다.

## :checkered_flag: Definition of Done (DoD)
- [ ] `status`의 생애주기 값 지정 및 Json `responseData` 설정
- [ ] FK 제약 조건에서 Optional(`?`) 확인

## :construction: Dependencies & Blockers
- **Depends on:** `#DB-002`(BuyerCompany)
- **Blocks:** `#API-022`(요청), `#MOCK-006`
