---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 사양
title: "[NFR] NFR-003: Supabase PostgreSQL 배포 환경 초기화"
labels: 'infra, db, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [NFR-003] Cloud Database (Supabase) 인스턴스 구축 및 Prisma 연동
- 목적: 로컬 SQLite를 넘어서, 상용 트래픽을 견디고 Edge Functions와 궁합이 맞는 Supabase의 PostgreSQL 풀을 생성하여 ORM(Prisma)과 밀봉 결합한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 제약사항: `06_SRS-v1.md#CON-13` (RDBMS 한정 정책)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] Supabase Dashboard 플랫폼 내 새 프로젝트 Instance 생성.
- [ ] `DATABASE_URL` 및 `DIRECT_URL` (Connection Pooling 분리 요건) 획득 및 Vercel/로컬(`.env`) 에 패스.
- [ ] SSL 연결 파라미터(`?pgbouncer=true` 등)를 Prisma 스키마 설정에 맞게 적용.
- [ ] (옵션) Supabase 내의 Storage 버킷(Bucket) 초기 생성 (FC-024용).

## :checkered_flag: Definition of Done (DoD)
- [ ] `npx prisma migrate dev` 혹은 `deploy` 가 에러 없이 리모트 DB에 테이블을 꽂아 넣는가?
- [ ] 서버 배포본의 API가 클라우드 DB의 데이터를 정상적으로 CRUD 해내는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-001, #NFR-002
- Blocks: 향후 DB 연관된 모든 실서버 구동부
