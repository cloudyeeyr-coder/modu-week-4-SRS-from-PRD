---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 사양
title: "[NFR] NFR-014: 중앙 통제형 RBAC(역할 기반 접근 제어) 미들웨어 강화"
labels: 'infra, security, auth, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [NFR-014] API Route / Server Actions 레벨의 철벽 Admin 방어 미들웨어
- 목적: 돈을 다루거나(에스크로 방출), 심판장의 역할(분쟁 강제 전환)을 하는 핵심 로직들에 일반(Buyer, SI) 계정이 세션을 변조해 접근하지 못하도록, 어플리케이션 전반을 두르는 Role 방어 래퍼(Wrapper)나 미들웨어 강화 구축.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 제약사항: `06_SRS-v1.md#REQ-NF-016`
- 연계망: `FC-003`

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] **NextAuth / Auth.js 활용**: JWT 내의 `role` 클레임(Claim) 여부를 `middleware.ts` 레벨에서 1차 가드. (`/api/admin/*` 경로에 대한 통합 막기).
- [ ] **Server Action 가드 함수 작성**: 
  - `adminAction` 같은 고차 함수(HOC/Wrapper)를 `lib/action-guards.ts` 에 공용으로 선언.
  - 내부 로직에서 비동기로 `currentUser.role === 'admin'` 검수 후, 실패 시 `throw new Error("Unauthorized")`.
- [ ] (필수 적용) 에스크로 입금 확인, 방출 결정, 분쟁 중재 로직 등 `FC-006, 009, 010` 등에 강제 이식 여부 전수 검수.

## :checkered_flag: Definition of Done (DoD)
- [ ] 일반 세션 브라우저에서 Postman 등 도구로 에스크로 방출 Endpoint를 타격했을 때, DB로 가기 전에 403 Forbidden 으로 튕겨내는가?

## :construction: Dependencies & Blockers
- Depends on: #FC-003 (Role 체계)
- Blocks: None
