---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[API] API-010: AS 엔지니어 배정 (assignEngineer) DTO 및 수칙 명세"
labels: 'api, backend, as, admin, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [API-010] AS 엔지니어 배정 (`assignEngineer`) Server Action DTO
- 목적: 접수된 AS 티켓에 마스터 풀(Pool)의 엔지니어를 맵핑해 줄 호출 규격 및 규칙(권한 등) 모델 작성.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `06_SRS-v1.md#6.1` (API No. 7)
- 관련 요구사항: `REQ-FUNC-007` (배정 규칙 및 시간 SLA)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] `AssignTargetSchema` Zod 정의: `ticketId`, `engineerId`
- [ ] 매칭 배정 시 활용될 "지역(Region)/역량" 필터링 인터페이스용 Query/Param 구조 (옵션) 추가 고려
- [ ] Admin 또는 시스템 봇(자동) 전용 호출 규격임을 주석 및 권한 가이드화 
- [ ] 에러 코드 목록 정의 (`ENG_NOT_FOUND`, `ENG_NOT_AVAILABLE`, `INVALID_REGION`)

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 엔지니어 ID 필수성 점검
- Given: `ticketId` 만 있고 엔지니어의 `engineerId` 가 누락된 Payload
- When: 서버 액션을 콜함
- Then: Zod Validator 가 "배정 대상 엔지니어를 선택하세요" 등의 에러 객체를 뿌려 응답한다.

Scenario 2: 배정 거부 규격
- Given: 비가용(`isAvailable: false`) 상태로 표기된 엔지니어 ID가 넘어옴
- When: 모의 배정이 완료되었다고 과정함
- Then: 백엔드는 이 DTO 규격서에 정의된 에러 코드인 `ENG_NOT_AVAILABLE` 를 뿌리도록 확약된다.

## :gear: Technical & Non-Functional Constraints
- 제약/연동: AS 배정은 자동화(FC-012) 로직이 태워질 가능성도 높으므로, 클라이언트 측 폼 데이터로 넘어오는 DTO 체커와 시스템 내부에서 함수로 던져질 때의 Param 규격을 깔끔하게 Type 분리(또는 혼용 지원) 해 두는 것이 코드 품질에 좋다.

## :checkered_flag: Definition of Done (DoD)
- [ ] Ticekt, Engineer 연관 ID가 포함된 DTO가 구성되었는가?
- [ ] 에러 코드가 정의되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-007, #DB-017
- Blocks: #FC-012
