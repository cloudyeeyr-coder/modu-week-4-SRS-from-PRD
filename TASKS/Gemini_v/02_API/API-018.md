---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[API] API-018: 파트너 제안 발송 (sendPartnerProposal) DTO 및 5일 만료 기한 규격"
labels: 'api, backend, partnership, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [API-018] 파트너 제안 발송 (`sendPartnerProposal`) Server Action DTO
- 목적: 제조사가 타겟 SI 파트너에게 협력을 제안할 때 사용하는 데이터 구조로써 SRS 상의 5영업일 만료 기한을 시스템 레벨에 알리는 명세를 작성합니다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 보완 및 추출 근거: ERD 보완 (DB-013) 및 파트너 제안 발송 시퀀스 (`06_SRS-v1.md#REQ-FUNC-030`)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 입력 `ProposalPayloadSchema` 정의: 대상 식별자 `siPartnerId`, 그리고 메시지 `proposalMessage`(옵션)
- [ ] 발신자의 신원(`manufacturerId`)은 Session에서 꺼내서 주입 시키도록 권장하는 인터페이스 주석 작성
- [ ] DTO 수준의 만료 정책 명세: 호출 시 `deadline`은 자동으로 생성 날짜 + 5영업일(또는 캘린더 5~7일)로 세팅된다는 정책 주석 선언
- [ ] 응답 반환 설계: `{ success, data: PartnerProposal }`
- [ ] 에러 정의: `PROPOSAL_ALREADY_EXISTS_PENDING` (중복 제안 방지)

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 제안 폼 Zod 파싱
- Given: 올바른 SI 타겟팅 값
- When: 서버 액션을 호출함
- Then: 식별 파라미터가 유효성 검증을 통과하여 `siPartnerId` 값으로 올바르게 인식되어야 한다.

Scenario 2: 기 보낸 제안 방어
- Given: 대상 SI 에게 이미 보낸, 그리고 상태가 `pending` 인 제안이 DB 상 존재함 (DB 수준 제약 또는 로직 체크)
- When: 다시 동일한 액션을 요청함
- Then: 어플리케이션(Action) 계층에서 타입에 정의된 `PROPOSAL_ALREADY_EXISTS_PENDING` 에러를 던져 중복 알림 테러를 차단한다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 이 인터페이스는 단지 DB에 제안 이력을 넣을 뿐 아니라 백그라운드에서 SI에게 즉각 알림을 발송(API-025 연동)하도록 로직 계층(FC-018)에 지시하게 되므로 가벼운 구조를 띄어야 한다.

## :checkered_flag: Definition of Done (DoD)
- [ ] Zod 기반 `siPartnerId` 검증 스키마가 선언되었는가?
- [ ] 중복 거절 용 에러 코드가 정의되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-013
- Blocks: #FC-018, #UI-009
