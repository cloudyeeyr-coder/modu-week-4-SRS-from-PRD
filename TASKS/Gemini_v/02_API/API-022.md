---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[API] API-022: 수기 견적 요청 (requestManualQuote) DTO 및 리드 상태 명세"
labels: 'api, backend, raas, quote, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [API-022] 수기 견적 요청 (`requestManualQuote`) Server Action DTO
- 목적: 수요기업이 RaaS/로봇 구매에 대해 맞춤형 견적을 플랫폼 운영팀(Admin)에게 송신하기 위한 폼 규격을 선언합니다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `06_SRS-v1.md#6.1` (API No. 19)
- 관련 요구사항: `REQ-FUNC-020`

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 입력 `ManualQuoteRequestSchema` Zod 처리: 
  - `modelCode`(혹은 `modelName`), `quantity`(최소 1 초과 정수), `durationMonths`, `contactInfo` (회신 연락처, 정규식 방어), `buyerMemo` 구비
- [ ] Server Action 상태 전이 명세: 해당 요청 접수 시 QuoteLead 상태를 즉각 `pending` 으로 발의(Initiate)한다는 규칙 명세.
- [ ] 응답 DTO: `{ success: true, leadId }` 및 접수 성공 멘트 포맷팅.
- [ ] 에러 목록: `INVALID_CONTACT_INFO`, `LEAD_CREATION_FAILED`

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 연락처 유효성(RegEx) 보호
- Given: 숫자나 특수문자가 엉망으로 기재된 `contactInfo`를 담은 폼
- When: 서버 액션을 발송함
- Then: Zod Validator 가 정규식 필터링을 통해 400에 상응하는 리턴과 폼 에러(예: "올바른 연락처 형식이 아닙니다")를 응답.

Scenario 2: 리드 생성 및 식별자 반환
- Given: 정상적인 수기 견적 입력 폼
- When: 서버 액션이 정상 처리 후 응답함
- Then: 이 DTO 명세에 따라 반환 값 속에 생성된 리드의 식별키(`leadId`)가 포함되어 고객용 "요청 완료 / 접수 번호" 페이지(UI-011)로 안전하게 연결될 수 있다.

## :gear: Technical & Non-Functional Constraints
- 구조 연계: 해당 요청 접수 후 관리자에게 메일/슬랙 알림을 날리는 로직 레이어 작업(FC-022)을 위해, DTO 내부에 추가 메타데이터 패싱 여부를 감안하여 넉넉한 Type 구조를 잡을 것.

## :checkered_flag: Definition of Done (DoD)
- [ ] Zod Object 파싱 처리가 작성되었는가?
- [ ] 상태 `pending` 전이를 위한 안내 주석 및 `contactInfo` 핸들 구조 작성 여부 확인.

## :construction: Dependencies & Blockers
- Depends on: #DB-012
- Blocks: #FC-022, #UI-011
