---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-022: 수기 견적(Quote) 접수 및 노티 발송 연계 (Write)"
labels: 'backend, context, quote, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-022] RaaS & 수기 견적 리드 목록 저장 트랜잭션
- 목적: 수요기업 담당자가 수기 폼으로 접수한 견적 문의 데이터를 유실 없이 DB에 적재(`QuoteLead`)하고 플랫폼 운영팀의 시스템에 핑(Ping)을 날리는 접수 로직.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-020`
- 연동 DTO: API-022

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] API-022 `ManualQuoteRequestSchema`를 거친 깨끗한 파라미터 수신.
- [ ] CUD 실행: `QuoteLead` 테이블에 기본 정보 및 `status = 'pending'` 삽입. 
- [ ] 비동기 파생: 생성된 `QuoteLead.id` 를 들고 어드민 알림 발송 모듈(API-025 혹은 API-026 Slack Webhook)을 던지는 `Promise.resolve` 스레드백 연계.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 데이터 무결성 보존 및 비동기 알림 연결
- Given: 올바른 형태의 `contactInfo`를 담은 고객 요청 (전화번호)
- When: FC 레이어가 이를 DB에 저장하는데 성공함
- Then: 사용자는 1~2초 내에 "성공적으로 접수되었습니다" 화면(UI-011)을 보게 되며, 관리자는 5초 이내에 자신의 슬랙에서 해당 알림을 관측할 수 있게 백그라운드로 작동된다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 알림 발송은 서드파티 통신의 연계이므로 메인 DB Insert 트랜잭션과 생사고락(await)을 같이 하지 않게 반드시 분리하여야 함 (DB 타임아웃 방어).

## :checkered_flag: Definition of Done (DoD)
- [ ] DB Create 가 성공적으로 작성되었는가?
- [ ] 비동기 노티 발급 래퍼가 묶여 있는가?

## :construction: Dependencies & Blockers
- Depends on: #API-022, #DB-012
- Blocks: #FQ-008
