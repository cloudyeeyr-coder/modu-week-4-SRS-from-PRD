---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-029: Admin 견적 응답 등록 트랜잭션 (Write)"
labels: 'backend, context, quote, admin, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-029] 관리자 수기 견적(`QUOTE_LEAD`) 응답 등록
- 목적: 고객이 보낸 RaaS 문의 건에 대해 Admin이 메일, 전화 연락 후 "이렇게 대응했습니다" 라고 결과를 남기어 상태를 업데이트 하는 처리 과정.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#3.4.6` (RaaS 구독 흐름, 고객 응답)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] Admin 권한 필터 검증.
- [ ] 타겟 리드 확인: `QuoteLead` 테이블에서 해당 ID의 리드 꺼내오기.
- [ ] 폼 파라미터 (어드민 회신 내용, `JSON` 형태의 견적 요약 옵션) Zod 수신.
- [ ] Update 쿼리 실행:
  - `status` = `responded` 전환
  - `responseData` = 제공된 JSON 덤프 데이터
  - `respondedAt` = Date.now()
- [ ] (옵션) 상태 업데이트 완료 후 수요기업에게 "견적이 안내 처리되었습니다" 자동 알림망(FC-023) 큐 발행.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 응답 등록 및 상태 마킹
- Given: 아직 `pending` 이거나 `in_progress` 상태인 견적 요청 리드
- When: 어드민 대시보드(Admin UI)를 통해 답변을 Submit 함
- Then: 이 시스템이 무사히 상태를 `responded`로 변경하고, 회신 데이터(`responseData`)를 결합해 차후 Admin이 클릭했을 때 볼 수 있도록 Read(FQ-008) 컴포넌트에 공급한다.

## :gear: Technical & Non-Functional Constraints
- 연계/제약: 이 CUD 트랜잭션이 성공하면 더 이상 Admin 대시보드의 "대기 중 문의" 배지가 떠서는 안되므로 클라이언트 캐시(revalidate) 무효화가 진행되어야 합니다.

## :checkered_flag: Definition of Done (DoD)
- [ ] Update 구문 및 JSONB 데이터 주입이 설계되었는가?
- [ ] 권한 가드(RBAC Admin 전용)가 걸려있는가?

## :construction: Dependencies & Blockers
- Depends on: #FC-022, #DB-012
- Blocks: None
