---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-012: AS 엔지니어 지역 및 가용성 기반 배정 룰 (Write)"
labels: 'backend, context, as, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-012] AS 엔지니어 동적 단건 배정 (`assignEngineer` 파이프라인)
- 목적: 접수된 티켓에 대하여 시스템 관리자(또는 자동자)가 엔지니어를 매칭할 때, 해당 엔지니어가 근무 가능한 상황(`isAvailable`)인지, 시간 겹침은 없는지를 더블 체크하는 할당 트랜잭션.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-007`
- 종속 DTO: API-010

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] DTO의 `ticketId` 와 `engineerId` 획득.
- [ ] 조건 검사(Precondition):
  - 티켓의 상태가 `reported` (아직 미배정) 인가?
  - 대상 `AsEngineer` 의 `isAvailable` 이 `true` 인가?
- [ ] 트랜잭션 구동:
  - 티켓 데이터에 `engineerId` 할당 및 상태를 `assigned` 로 업데이트.
  - 타임스탬프 `assignedAt = Date.now()` 갱신.
- [ ] 배정 완료 알림(SMS/어드민)을 큐에 적재(API-025 연계).

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 가용 불가 엔지니어 배정 블록
- Given: 오늘 휴가중(`isAvailable: false`)이거나 타 지역의 엔지니어 레코드(MOCK-005)
- When: 어드민이 강제로 해당 엔지니어를 배정하는 액션을 콜함
- Then: 이 FC 검사 계층에서 해당 레코드 속성을 읽은 뒤, `ENGINEER_NOT_AVAILABLE` 이라는 에러를 던져 잘못된 배정을 막는다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 자동 배정 스크립트(CRON) 가 추후 이 모듈을 똑같이 호출할 것이므로, FC 함수는 Request 객체를 직접 받기 보다 순수 파라미터 함수 랙핑(`async function assign(ticketId, engId)`) 형태를 취하여 재사용성을 극대화시켜라.

## :checkered_flag: Definition of Done (DoD)
- [ ] 가용성 상태 검증 쿼리 포함 구현 완료
- [ ] 티켓 상태(assigned) 및 시간 데이터 업데이트 쿼리 구성 완료

## :construction: Dependencies & Blockers
- Depends on: #API-010, #DB-007, #DB-017
- Blocks: #CRON-004, #UI-007
