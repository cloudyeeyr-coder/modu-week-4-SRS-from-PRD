---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-028: 유사 로봇 모델 추천 AI/규칙 알고리즘 구현 (Compute)"
labels: 'backend, compute, raas, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-028] 유사 모델 추천(Fallback) 검색 엔진 구현
- 목적: 수요기업이 RaaS 계산기나 문의 시 DB에 존재하지 않거나 단종된 모델을 입력했을 때, 그냥 에러로 튕기는게 아니라 단가(`basePrice`)가 비슷한 타사의 3건의 모델을 응답해주기 위한 룰 엔진.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-021` (유사 모델 추천 로직)
- 연관 태스크: FC-020 (RaaS 연산 분기)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 오류 시점 분기 확인: FC-020 등 계산 모듈에서 모델 조회 `Prisma` 결과가 Null일 경우 이 함수를 브릿지 호출한다.
- [ ] 추천 알고리즘 로직 연산:
  - 입력받은 모델의 카테고리(`payload` 로 유추)나 혹은 시스템에 보관된 과거 모델 테이블을 참조하여 베이스 단가(`priceRange` 내외 20%)를 구함.
  - 제조사(`Manufacturer`)가 상이한 대안 모델 3개를 `RobotModel` 뷰에서 Select.
- [ ] `{ error: 'NOT_FOUND', suggestions: [RobotModelA, RobotModelB, RobotModelC] }` 형태로 패키징하여 리턴.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 추천 결과 보장
- Given: 단종된 로봇 "OLD-1" 모델 입력
- When: RaaS 계산기(FC-020) 구동 시 모델 식별 실패
- Then: 이 추천 엔진(FC-028)이 백업 가동되어, 비슷한 속성의 모델 3건의 옵션 데이터를 반환하여 사용자가 계산을 바로 이어갈 수 있게 돕는다. 

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 모델명 단순 오입력이 아니라, 아예 텍스트/자연어 검색 형태로 될 경우를 대비해 Prisma의 Full Text Search `search` 옵션이나, Vercel AI SDK(임베딩) 등을 활용할 유연성을 남길 것.

## :checkered_flag: Definition of Done (DoD)
- [ ] 대안 로봇 모델 추출 쿼리가 확립되었는가?
- [ ] API 반환 객체 규격에서 Suggestion 요소를 수용할 수 있게 설계되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-016
- Blocks: #FC-020
