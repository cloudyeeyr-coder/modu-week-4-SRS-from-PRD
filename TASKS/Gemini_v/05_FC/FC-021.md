---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-021: RaaS 비교 결과 PDF 렌더링 파이프라인 (Write)"
labels: 'backend, compute, raas, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-021] RaaS 비교 결과 데이터의 PDF 변환기
- 목적: FC-020 등 엔진을 거쳐 나온 계산 결과 혹은 프론트에서 넘어온 JSON 복합체를 파싱하여 고객이 보고서(기안) 로 쓸 수 있게 PDF 로 말아주는 모듈 셋업.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-019`
- 성능 제약: `REQ-NF-004` (PDF 생성 성능 3초 이내)
- 연계 모듈: API-021, FC-020

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 입력 파라미터 파싱 (수량, 기간, 적용 모델, 그리고 계산된 3개의 총 비용).
- [ ] React-PDF 혹은 JS기반 렌더러를 탑재하여 `TCO(총 소유비용)` 비교 테이블 컴포넌트 생성.
- [ ] ROI(투자 수익률)나 그래프 등의 시각적 요소가 필요할 경우, Canvas Base64 등을 넘겨받아 심는 브릿지 역할 수행 설정.
- [ ] 브라우저에서 다운로드 가능하게 Buffer 혹은 Blob 을 내뱉는 Endpoint 체결.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: NFR 퍼포먼스 달성
- Given: Vercel 배포 환경
- When: 비용 계산기 모달에서 "PDF 다운로드" 를 호출함
- Then: 콜드 스타트를 감안하더라도 무거운 Chromium 헤드리스 브라우저 대신, 가벼운 Stream 방식의 PDF 조립기가 구동되어 약속된 3초(p95) 내에 클라이언트로 진입한다.

## :gear: Technical & Non-Functional Constraints
- 설계 검토: 이 모듈의 부담을 덜기 위해 브라우저단(Client Side)에서 HTMLCanvas를 이용해 직접 PDF를 만드는 방향(jspdf 활용)을 프론트와 적극 타협하라. 

## :checkered_flag: Definition of Done (DoD)
- [ ] API-021 설계에 따른 파포먼스 패키지 설치 완료
- [ ] 에러를 던지지 않고 PDF 헤더(`application/pdf`)가 올바르게 반환되는가?

## :construction: Dependencies & Blockers
- Depends on: #API-021, #FC-020
- Blocks: #UI-010
