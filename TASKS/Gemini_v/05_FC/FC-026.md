---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-026: O2O 예약 가용 슬롯 제척 알고리즘 및 대기열 회피 로직"
labels: 'backend, context, o2o, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-026] O2O 슬롯 충돌/제척 회피 알고리즘 (Phase 2)
- 목적: 고객이 O2O 예약을 원할 때 빈 슬롯이 0건일 경우 에러 대신 "가장 가까운 가용 슬롯 일정"을 대안으로 찾아내어 추천해 주는 알고리즘 엔진을 구현.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-026` (슬롯 0건 대기 모델)
- 종속 쿼리: FQ-011 

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 입력 파라미터로 희망 날짜/시간대를 받음.
- [ ] 먼저 FQ-011 을 가동하여 해당 날짜의 가용 슬롯이 남아있는지 체크.
- [ ] 만약 슬롯 카운트가 `0` 인 경우(`SLOT_NOT_AVAILABLE` 에러 전):
  - 다음 날짜(+1일), 혹은 전후 3일의 반경을 돌며 `status === pending|confirmed` 가 아닌 빈 시간대를 쿼리함.
- [ ] 대안 시간대(예: "해당 일자는 예약이 꽉 찼습니다. 내일 14:00은 어떠신가요?") 객체 형태로 에러 Payload 에 함께 반환하여 프론트가 이를 랜더링 할 수 있게 함.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 대체 일정 제안(Suggestion)
- Given: 오늘/내일 수도권 매니저 예약 스케줄이 Full 로 차있음
- When: 수요기업이 캘린더에서 강제로 내일 예약 API를 쏨
- Then: 이 모델은 "400 Bad Request" 와 함께 `nearestAvailable: 'YYYY-MM-DD 13:00'` 값을 찾아내 응답함으로써 대기 예약 전환을 부드럽게 유도한다.

## :gear: Technical & Non-Functional Constraints
- 성능 제약: +3~4일 루프를 돌며 DB를 Hit하게 되므로 N+1 문제가 생기지 않도록 `date: { gte: now, lte: 7days }` 처럼 날짜 범위를 크게 줘서 한번의 쿼리 결과로 메모리에서 제척 연산을 끝내야 한다.

## :checkered_flag: Definition of Done (DoD)
- [ ] 슬롯 0건 시 대체 일정 제안 알고리즘이 작성되었는가?
- [ ] N+1 쿼리 최적화가 이루어져 있는가?

## :construction: Dependencies & Blockers
- Depends on: #FQ-011
- Blocks: None
