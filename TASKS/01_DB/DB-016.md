---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-016: ROBOT_MODEL 테이블 스키마 작성"
labels: 'database, schema, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-016] `ROBOT_MODEL` (로봇 모델 마스터 데이터) 스키마 작성
- 목적: RaaS 및 비용 계산 엔진에서 조회 및 추천 등에 구동될 '마스터 로봇 데이터(가격/제원 등)' 구조를 데이터베이스에 구현한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 보완 기초: RaaS 비용 계산 엔진 요구사항 (`06_SRS-v1.md#REQ-FUNC-018`, `REQ-FUNC-021`)
- 연관 기반: `Manufacturer` 모델 연결

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] `schema.prisma`에 `RobotModel` 엔티티 정의 
- [ ] 핵심 식별자 및 검색 인덱스용 `modelCode` (String, `@unique`) 추가 
- [ ] 외래키 처리: `manufacturerId` → `Manufacturer` 연관 관계 선언
- [ ] 정보 필드 추가: `modelName` (String), 계산 엔진 기초자료인 `basePrice` (Decimal) 지정
- [ ] 마이그레이션 파일 단위 독립 생성

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 계산 엔진 위한 기준 값 보장
- Given: 조회용 로봇 마스터 스키마
- When: 로봇 정보를 DB에 등록함
- Then: `basePrice` 모델을 금액 타입(Decimal/Float)으로 견고하게 유지하고 반환하여 가격 연산에서 런타임 오류가 나지 않도록 해야 한다.

Scenario 2: 제조사와 로봇 간 계층 구조 확보
- Given: 다수의 제조사와 여러 로봇 모델
- When: 관계 쿼리를 지시함
- Then: 로봇 모델 모델 안에 `Manufacturer` 외래키 구문이 정상 연결되어, 조회를 통한 추천/목록 렌더링에 적절하게 응답한다.

## :gear: Technical & Non-Functional Constraints
- 성능 제약: `modelCode` 컬럼에는 고유 인덱스가 반드시 형성되어있어야 함 (`@unique`), 사용자가 RaaS 계산기 상에서 모델 번호를 직접 입력할 때 탐색 성능을 보장하기 위함 (p95 ≤ 1,000ms 수준 대응).

## :checkered_flag: Definition of Done (DoD)
- [ ] RaaS 연산에 필수적인 단가 정보(`basePrice`)와 외래키(`Manufacturer`)가 스키마에 선언 되었는가?
- [ ] 마스터 모델용 DB 마이그레이션 명령어 성공 유무

## :construction: Dependencies & Blockers
- Depends on: #DB-004 (Manufacturer)
- Blocks: #API-020, #FC-028, #MOCK-006 (Seed)
