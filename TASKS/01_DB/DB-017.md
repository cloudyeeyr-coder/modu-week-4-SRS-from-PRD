---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[DB] DB-017: AS_ENGINEER 테이블 스키마 작성"
labels: 'database, schema, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [DB-017] `AS_ENGINEER` 테이블 스키마 작성
- 목적: AS 티켓의 자동 배정(SLA 목표) 대상이 되는 '로컬 AS 엔지니어' 들의 풀을 구성하고, 가용 여부를 체크하기 위한 기반 스키마를 구성한다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 보완 및 추출 근거: `06_SRS-v1.md#6.3.5` (AS 엔지니어 매칭 자동화 시퀀스)
- 연관 태스크: FC-012 (엔지니어 배정)

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] `schema.prisma` 에 `AsEngineer` 모델 정의
- [ ] 엔지니어 정보 속성 지정: `name` (String), 지역 필터링을 위한 `region` (String) 구성
- [ ] 매칭 알고리즘 재료 지정: 역량 태그 정보 관리용 `skills` (Json 또는 String)
- [ ] 자동 배정 전제 조건 상태값 명시: `isAvailable` (Boolean) 추가
- [ ] DB 단일 마이그레이션 작업 반영

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 가용 상태 전환 및 검색
- Given: 등록된 다수의 마스터 엔지니어
- When: Boolean 필드인 `isAvailable` 을 조건으로 쿼리함
- Then: DB 상 스키마가 불리언 형태를 지원하고 문제 없이 분리된 풀(가용자/비가용자)을 조회해낸다.

Scenario 2: 배정 및 티켓 매핑 호환
- Given: 기존 `AsTicket` 모델 내역
- When: AS 배정 처리(FC-012) 로직이 구동됨
- Then: 티켓 모델 쪽에서 엔지니어 참조 외래키(통상 `assignedEngineerId`)를 잡을 수 있도록 시스템적 앵커 식별자(PK uuid)가 온전히 선언되어야 한다. 

## :gear: Technical & Non-Functional Constraints
- 구조/확장성: 본 태스크에서는 엔지니어 풀 구성만 수행함. 단, `AS_TICKET` (DB-007) 측에서 이 테이블을 참조 할 수 있도록 외래키 선언(방향성)을 한 번에 검수해두는 것이 안전함.

## :checkered_flag: Definition of Done (DoD)
- [ ] AS 엔지니어 요구 스키마가 ORM 상 문제없이 렌더링/포맷 되는가?
- [ ] 마스터 모델 배포 마이그레이션이 테스트 DB에 성공적으로 작성/반영되었는가?

## :construction: Dependencies & Blockers
- Depends on: #DB-001
- Blocks: #API-010, #FC-012, #MOCK-005 (Seed)
