---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-005: 에스크로 계약 생성 프로세스 (Write)"
labels: 'backend, context, escrow, priority:high'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-005] 에스크로 계약 생성 (`createContract`) 트랜잭션 로직
- 목적: 수요기업 담당자가 수기 혹은 견적 기반으로 특정 SI 파트너와 진행할 로봇 프로젝트에 대해, 가상 에스크로 Contract의 첫 단추(`pending`)를 꿰는 쓰기(Write) 모듈.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-001` (계약 체결 및 에스크로 생성)
- 연관 DTO: API-003

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 파라미터 유효성: API-003 의 Zod `CreateContractSchema` 통과 데이터를 받음.
- [ ] 권한 체크(미들웨어와 별도): 현재 요청(세션)의 역할이 `buyer`인지, 그리고 `buyerCompanyId`와 세션의 소속 회사가 조작되지 않았는지 2중 방어 검증 로직 구현.
- [ ] 트랜잭션 시작
  - 1단계: `Contract` 추가(`status: pending`).
  - 2단계: 연동될 `EscrowTx` 생성(`state: pending`) 및 이력이 계약 레코드에 물렸음을 확인.
- [ ] 최종적으로 사용자에게 가상계좌(무통장입금 법인계좌) 문자열을 포함하여 Response 리턴.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 세션/바디 불일치 권한 위반 방어
- Given: 해커가 내 세션은 Buyer `A` 인데, Body 파라미터를 조작해 Buyer `B` 회사의 아이디로 결제를 위조 시도함
- When: 서버 액션을 콜함
- Then: 이 FC 계층에서 `throw Error("UNAUTHORIZED_ACTION")`을 터뜨려 고의적인 덮어쓰기를 사전에 블럭한다.

Scenario 2: 트랜잭션 정상 수행
- Given: 올바른 계약 체결 폼
- When: FC 로직이 성공적으로 커밋됨
- Then: 계약 테이블과 예치 트랜잭션 테이블 이 양 쪽에 동시에 데이터가 삽입되어야 하며, Admin(운영진) 측에만 보이는 모니터링 목록(FQ-007)에 노출될 자격 요건을 갖춘다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 금액(`totalAmount`)은 실 금융 서비스 확장성을 고려해 정수타입이거나 Decimal 타입이 다뤄지는데, JavaScript 고질병인 부동소수점 오리가 나지 않도록 Prisma Decimal 파서나 안전 라이브러리를 동원해 DB에 집어넣어라.

## :checkered_flag: Definition of Done (DoD)
- [ ] Contract - EscrowTx 동기화 트랜잭션 쿼리 구성 완료
- [ ] 계좌 안내 메시지 반환 로직 구성 

## :construction: Dependencies & Blockers
- Depends on: #API-003, #FC-003
- Blocks: #FC-006
