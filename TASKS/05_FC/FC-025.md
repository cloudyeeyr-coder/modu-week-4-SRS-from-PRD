---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-025: Phase 2 O2O 방문 예약 생성 트랜잭션 (Write)"
labels: 'backend, context, o2o, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-025] O2O 방문 예약 신청/생성 
- 목적: Phase 2의 스펙으로, 수요기업이 특정 슬롯의 방문 상담을 요구했을 때 DB에 예약 건(`pending`)을 생성하는 쓰기 로직이다.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-023` (Phase 2 스펙)
- 종속 DTO: API-023

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 폼 파라미터(날짜, 대상 시간대 슬롯, 권한 ID)를 수령.
- [ ] 동시성 충돌(Race Condition)을 막기 위해 FC-026 모듈(충돌 방어 엔진)을 Call하여 한번 더 현재 시점 잔여 슬롯 조회.
- [ ] 무사 통과 시 `O2oBooking` 테이블 Insert (`status: pending`).
- [ ] 결과 URL(`success, bookingId`) 반환 및 예약 대기 알림 큐 발행.

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: 동시 접속자 경합 예약 차단
- Given: 동일한 화요일 14시 슬롯에 대해 A회사와 B회사가 0.1초 차이로 동시 클릭/예약 시도
- When: 트랜잭션 핸들러 가동됨
- Then: 이 FC 레이어 내부 혹은 DB 단의 슬롯 고유 제약조건(예: `date` + `timeSlot` Unique)에 의해 뒷선으로 날아온 B회사의 쿼리는 Rollback 에러가 나며, 프론트로 "이미 선점된 예약입니다." 피드백이 주어진다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 해당 로직은 MVP에 노출되지 않도록 폴더 레벨 분리 혹은 피쳐플래그 스위칭이 가능하도록 구성하는 것에 유의하라.

## :checkered_flag: Definition of Done (DoD)
- [ ] 예약 레코드 생성 쿼리가 무사히 준비되었는가?
- [ ] 동시 경합 방어(Unique Constraints)가 씌워져 있는가?

## :construction: Dependencies & Blockers
- Depends on: #API-023, #DB-010
- Blocks: None
