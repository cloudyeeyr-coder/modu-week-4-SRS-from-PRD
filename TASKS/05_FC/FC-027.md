---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-027: O2O 방문 보고서 제출 트랜잭션 (Phase 2)"
labels: 'backend, context, o2o, priority:low'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-027] O2O 방문 보고서 등록 (`submitVisitReport`) 및 상태 종결파이프라인
- 목적: 현장 매니저가 작성을 마친 O2O 상담 데이터를 수신받아 JSON 필드에 구겨 넣고 예약 상태를 최종 단계(`completed`)로 잠그는 로직.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-025`
- 연관 DTO: API-024

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] API-024 의 `VisitReportSchema` 입력값 획득 및 유효성(Zod JSON) 검증.
- [ ] `O2oBooking` 예약 레코드 상태 확인 (`status: confirmed` 및 매니저 본인 여부 확인).
- [ ] 트랜잭션 실행:
  - `status` = `completed` 로 종결 
  - `reportContent` = 입력된 JSON 객체 적재.
- [ ] (옵션 파생) 등록 완료 시 수요기업 이메일로 "보고서가 도착했습니다" 푸시(API-025).

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: JSONB 페이로드 보안
- Given: 매니저 앱에서 보고서를 작성하는데, 추천 파트너(`recommendedSiIds`) 배열에 외래키(UUID)가 아닌 이상한 스트링이 들어옴
- When: 트랜잭션이 구동함
- Then: JSON 모델 저장이라도 허술하게 넘기지 않고 DB 저장 이전에 Zod 규격대로 튕겨내 무결성을 지킨다.

## :gear: Technical & Non-Functional Constraints
- 구조 제약: 해당 테이블 업데이트 후엔 데이터가 클라이언트 컴포넌트로 자주 송신될 것이므로, 빈 배열을 핸들링하여 `null` 레퍼런스 에러가 나지 않게 DTO 방어를 수행. (MVP는 Phase 2).

## :checkered_flag: Definition of Done (DoD)
- [ ] 상태 전이가 `completed` 로 안전히 넘어가는가?
- [ ] JSONB 필드 저장을 위한 파싱이 원만히 수행되는가?

## :construction: Dependencies & Blockers
- Depends on: #API-024, #FC-025
- Blocks: None
