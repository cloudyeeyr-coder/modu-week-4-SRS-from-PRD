---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[FC] FC-013: AS 완료 처리 및 24H SLA 판정 마감 로직 (Write)"
labels: 'backend, context, as, priority:medium'
assignees: ''
---

## :dart: Summary
- 기능명: [FC-013] AS 티켓 완료 처리(`resolveTicket`) 및 SLA 자동 판정 갱신 파이프라인
- 목적: 현장 엔지니어/운영팀이 조치 완료 시점을 선고할 때, 접수시간(`reportedAt`)과 대조를 수행하여 회사가 규정한 '24시간 내 해결' 법령을 달성했는지(`slaMet` = true/false) 영구히 DB에 기록하는 마감 처리.

## :link: References (Spec & Context)
> :bulb: AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 관련 요구사항: `06_SRS-v1.md#REQ-FUNC-008` (SLA 조건)
- 종속 DTO: API-011

## :white_check_mark: Task Breakdown (실행 계획)
- [ ] 티켓의 기존 데이터(status, reportedAt) Fetch 
- [ ] 비즈니스 엔진(SLA Check): `resolvedAt`(현재 시각) 과 `reportedAt`의 차이를 계산하여 24시간(`1000 * 60 * 60 * 24` ms) 미만인지 연산.
- [ ] 트랜잭션 구동:
  - `status` = `resolved` 로 닫힘
  - `resolvedAt` = Date.now() 병기
  - `slaMet` = 위 로직 연산 결과에 따른 boolean 값 삽입 
  - (옵션) `resolutionDetail`(엔지니어 보고) 등 정보 Update 

## :test_tube: Acceptance Criteria (BDD/GWT)
Scenario 1: SLA 24H 경과 지각 보고
- Given: 이틀 전(48시간 경과) 접수된 티켓 데이터 (MOCK-005 기반)
- When: 엔지니어가 완료 액션을 쏨
- Then: 이 FC 파이프라인이 SLA 공식을 계산해낸 뒤 `slaMet: false` 로 낙인(Update)을 찍어서 대시보드 경보망(FQ-010)이 작동하도록 한다.

## :gear: Technical & Non-Functional Constraints
- 기능/유지보수: 날짜 차이 계산은 향후 명절/주말 등 영업일 산출 기능으로 고도화 될 수 있으니, 코드 내부에 `const isSlaMet = calculateSla(reported, resolved)` 와 같은 유틸 함수로 분리해두어 의존성을 낮출 것.

## :checkered_flag: Definition of Done (DoD)
- [ ] SLA 산출 조건 함수가 분리 작성되었는가?
- [ ] 상태 전이가 `resolved` 로 정상 반영되는가?

## :construction: Dependencies & Blockers
- Depends on: #API-011, #DB-007
- Blocks: #FQ-010
